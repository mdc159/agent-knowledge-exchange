# Continue Fleet Configuration Here

The authoritative fleet handoff is:

`knowledge/hermes/NEXT-SESSION-HANDOFF.md`

Read it and the linked inventory before changing fleet nodes. Continue from the
verified state instead of restarting discovery, preserve provider recovery
paths, and update the inventory as live state changes.
