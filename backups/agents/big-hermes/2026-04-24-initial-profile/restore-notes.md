# Restore Notes

To restore this profile:

1. recreate the runtime home on the target node
2. restore the curated defining files only
3. reapply secrets from the external secret source
4. verify the pinned model/provider
5. verify the service or launcher path
