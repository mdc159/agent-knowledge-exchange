# Agent Backups

Store milestone snapshots only.

Path format:

`backups/agents/<agent-name>/<YYYY-MM-DD>-<milestone>/`

Each snapshot should contain:

- the curated defining files
- `summary.md`
- `restore-notes.md`
